# shellcheck shell=bash
# shellcheck disable=SC2034,SC2154

# Maintainer: Chmouel Boudjnah <chmouel@chmouel.com>
# THIS FILE WAS GENERATED BY A SCRIPT. DO NOT EDIT!

pkgname='snazy-bin'
pkgver=%VERSION%
pkgrel=1
pkgdesc='snazy - a snazzy json log viewer'
url='https://github.com/chmouel/snazy'
arch=('aarch64' 'x86_64')
license=('Apache 2.0')
provides=('snazy')
conflicts=('snazy')

source_aarch64=("${pkgname}_${pkgver}_aarch64.tar.gz::%LINUX_ARM_URL%")
sha256sums_aarch64=('%LINUX_ARM_SHA256%')

source_x86_64=("${pkgname}_${pkgver}_x86_64.tar.gz::%LINUX_URL%")
sha256sums_x86_64=('%LINUX_SHA256%')

package() {
	install -Dm755 "./snazy" "${pkgdir}/usr/bin/snazy"

	# license
	install -Dm644 "./LICENSE" "${pkgdir}/usr/share/licenses/snazy/LICENSE"

	# completions
	mkdir -p "${pkgdir}/usr/share/bash-completion/completions/"
	mkdir -p "${pkgdir}/usr/share/zsh/site-functions/"
	mkdir -p "${pkgdir}/usr/share/fish/vendor_completions.d/"

	${pkgdir}/usr/bin/snazy --shell-completion=bash >"${pkgdir}/usr/share/bash-completion/completions/snazy"
	${pkgdir}/usr/bin/snazy --shell-completion=zsh >"${pkgdir}/usr/share/zsh/site-functions/_snazy"
	${pkgdir}/usr/bin/snazy --shell-completion=fish >"${pkgdir}/usr/share/fish/vendor_completions.d/snazy.fish"

}

# vim: ft=bash
